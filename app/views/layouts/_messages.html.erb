<script>
<% flash.each do |name, msg| %>
    <% if msg.is_a?(String) %>
        var options = { message: '<%= msg %>'};
        var settings = {type: 'success', allow_dismiss: true};
        flash_notify(options, settings);
    <% end %>
<% end %>
</script>
