<script>
<% flash.each do |name, msg| %>
    <% if msg.is_a?(String) %>
        var options = { message: '<%= msg %>'};
        var settings = {type: 'success', allow_dismiss: false};
        $.notify(options, settings);
    <% end %>
<% end %>
</script>
