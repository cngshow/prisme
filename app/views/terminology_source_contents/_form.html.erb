<%= form_for(@terminology_source) do |f| %>
  <% if @terminology_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@terminology_source.errors.count, "error") %> prohibited this terminology_source_content from being saved:</h2>

      <ul>
      <% @terminology_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
